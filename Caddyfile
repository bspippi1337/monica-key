{$MONICA_KEY_DOMAIN:localhost} {
    encode zstd gzip
    reverse_proxy relay:4242

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        X-Frame-Options "DENY"
        Permissions-Policy "geolocation=(), microphone=(), camera=()"
        -Server
    }
}
